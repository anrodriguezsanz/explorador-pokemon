import type { CSSProperties } from 'react';

export const styles = {
  container: {
    padding: '40px',
    maxWidth: '900px',
    margin: '20px auto 0 auto'
  } as CSSProperties,

  spinnerContainer: {
    textAlign: 'center',
    marginTop: 100
  } as CSSProperties,

  buttonContainer: {
    marginTop: 20,
    textAlign: 'center'
  } as CSSProperties
};
